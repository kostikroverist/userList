# userList 
## running front-end 
    For installing angular
    ``` bash
    npm install -g @angular/cli
    ```
    save npm modules 
    ```bash
    npm install
    ```
    run app
    ``` bash
    npm run start
    ```
    or 
    ``` bash
    ng serve -o
    ```
## running back-end

