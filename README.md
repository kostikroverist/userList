# userList 
## running front-end 
  npm install -g @angular/cli for installing angular
 (get up) save npm modules
    ```cmd
    npm install
    ```
  run app
    ``` cmd
    npm run start
    ```
    or 
    ``` cmd
    ng serve -o
    ```
## running back-end

