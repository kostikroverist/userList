# userList 
## running front-end 
    For installing angular
    ```terminal
    npm install -g @angular/cli
    ```
    save npm modules 
    ```terminal
    npm install
    ```
    run app
    ```terminal
    npm run start
    ```
    or 
    ```terminal
    ng serve -o
    ```
## running back-end

