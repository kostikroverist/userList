# userList 
## running front-end 
    For installing angular
    ```cdm
    npm install -g @angular/cli
    ```
    save npm modules 
    ```cdm
    npm install
    ```
    run app
    ```cdm
    npm run start
    ```
    or 
    ```cdm
    ng serve -o
    ```
## running back-end

